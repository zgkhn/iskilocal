@page
@model IndustrialDataManagement.Pages.Plcs.DeleteModel
@{
    ViewData["Title"] = "PLC Sil";
}

<div class="container py-4">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card shadow-sm border-0 rounded-4">
                <div class="card-body p-4">
                    <h3 class="text-danger mb-3"><i class="bi bi-trash3 me-2"></i>PLC Silme Onayı</h3>
                    <p class="text-muted">Aşağıdaki PLC cihazını silmek istediğinizden emin misiniz? <strong>Bu işlem PLC'ye bağlı tüm tabloları ve tag'leri de silecektir!</strong></p>
                    
                    <div class="bg-light p-3 rounded-3 mb-4">
                        <div class="mb-2"><strong>PLC Adı:</strong> @Model.PlcToDelete?.Name</div>
                        <div class="mb-2"><strong>IP Adresi:</strong> @Model.PlcToDelete?.IpAddress</div>
                        <div><strong>Protokol:</strong> @Model.PlcToDelete?.Protocol</div>
                    </div>

                    <form method="post">
                        <input type="hidden" asp-for="PlcToDelete!.Id" name="id" />
                        <div class="d-flex gap-2">
                            <button type="submit" class="btn btn-danger px-4">Evet, Sil</button>
                            <a asp-page="./Index" class="btn btn-light px-4">Vazgeç</a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
